title: Media Library File

fields:
  category:
    label: Category
    type: select
    required: false
    options:
      type: query
      query:
        page(option('yngrk.media-library.uid', 'media-library')).categories.split

  alt:
    label: Alt text
    type: text

  caption:
    label: Caption
    type: textarea
